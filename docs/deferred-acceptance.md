# Deferred ?????Acceptance Lock?

> ?????????? GBuffer RT2 / Lighting cache key / Skybox kind ???? bump key **?**?????  
> ???2026-07-23 · ?????`gbuffer_fill_v7` / `lighting_v26` / `skybox_v2`  
> ??????[`short-term-plan.md`](short-term-plan.md) · FG ???[`frame-graph-mvp.md`](frame-graph-mvp.md)

## ?????Deferred?

`Shadow ? Skybox ? GBuffer ? Lighting ? Transparent ? PostProcess ? UI`

Editor?`AY_DEFERRED=1`?

## ??????????????

| ??? | ?? |
|--------|------|
| `gbuffer_fill_v7_worldpos_rgba16f` | RT2 = worldPos RGBA16F??? PCF ??? |
| `lighting_v26_mix_vec2_overloads` | ?/?? + IBL + atlas ???AYShader `uniform T[N]` + `mix(vec2,?)` |
| `skybox_v2_vec4_skykind` | equirect / cube ????`skyKind` ? vec4 |
| `dirs+colors+params+spotDir` | P5.5 B field-split ??????? dirs+colors? |
| `Skybox equirect + IBL envCube ready` | ?? = ???IBL = ?? cube?**??** `kind=CubeMap` ?? equirect? |
| `sceneLights=4` + `point+spot` | keyDir + fillDir + point + spot?key ?? pre-C/`u_lightViewProj` |
| `ShadowPass` ? `sampleReady=1` | ?? map ????`draws=` ?? atlasSlots??? 0? |
| `PostProcessPass` ? `gamma=2.2` `tonemap=2` | ACES + ?? gamma |

## ?????????

1. **GBuffer RT2** = `vec4(worldPos, 1.0)` + FBO **RGBA16F**??? motion NDC / ?? RGBA8??  
2. **Lighting ??**?? `ctx.shadowPass`?worldPos ?? `sample(gbufferMotion).xyz`?  
3. **UBO ???**?? `dirs`??? `record`????? `params` / `spotDir`?  
4. **?? uniform** ?? `uniform vec4`?FS ? `.x`????? `uniform float`??  
5. **?? vs IBL**?`SkySourceKind::Equirect` + `setSkySourceCube(handle)` ????`skyKind` ? kind?`cubeActive` ?? cube handle?  
6. **??** cache-key ???? live `k*CacheKeyCStr`??? mirror ????

## Editor ??????

```cpp
setPostProcessGamma(2.2f);
setPostProcessTonemapMode(TonemapMode::ACES);
setAmbientStrength(0.85f);  // IBL diffuse ?????????
```

?/????? `lights[0]`??? key shadow ????

## Transparent

`AYRenderSystem` ??????? `DrawItem::sortKey`?????????????????????????????

## ?????

????? ? ?????? `gbuffer_fill_v7` + Lighting live key??? `v26`??  
?????? ? ???? `kind=CubeMap` ?? equirect?  
??/????? ? ???????? `dirs+colors`?? params??  
? Lighting acquire failed ? ? AYShader?`uniform T[N]` / `mat4` let / `mix(vec2,?)`??
